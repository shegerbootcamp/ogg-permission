# templates/configmap.yaml

apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "goldengate.fullname" . }}-config
  labels:
    app: {{ include "goldengate.name" . }}
    chart: {{ include "goldengate.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
